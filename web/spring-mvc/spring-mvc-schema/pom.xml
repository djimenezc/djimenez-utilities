<?xml version="1.0"?>
<project
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
	xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<artifactId>spring-mvc</artifactId>
		<groupId>com.djimenez.utilities.web</groupId>
		<version>1.0-SNAPSHOT</version>
	</parent>
	<groupId>com.djimenez.utilities.web.spring-mvc</groupId>
	<artifactId>spring-mvc-schema</artifactId>
	<version>1.0-SNAPSHOT</version>
	<name>spring-mvc-schema</name>
	<url>http://maven.apache.org</url>
	<dependencies>
	</dependencies>

	<build>
		<defaultGoal>install</defaultGoal>
		<plugins>
			<plugin>
				<artifactId>maven-antrun-plugin</artifactId>
				<executions>
					<execution>
						<id>test</id>
						<phase>process-test-resources</phase>
						<goals>
							<goal>run</goal>
						</goals>
						<configuration>
							<tasks unless="maven.test.skip">
								<property name="compile_classpath" refid="maven.compile.classpath" />
								<property name="runtime_classpath" refid="maven.runtime.classpath" />
								<property name="test_classpath" refid="maven.test.classpath" />
								<property name="plugin_classpath" refid="maven.plugin.classpath" />

								<echo level="info">Loading test databases</echo>

								<ant antfile="${ant.build.dir}/build-parent.xml" target="database-load-test">
									<property file="${ant.target.dir}/build.${user.name}.properties" />
									<property file="${ant.target.dir}/build.properties" />
									<property name="parent.dir" value="${ant.build.dir}"/>
									<property name="parent.target.dir" value="${ant.target.dir}"/>
									<property name="bbdd.dir" value="./target/classes/schemas"/>
								</ant>

								<echo level="info">Finish load test databases</echo>

							</tasks>
						</configuration>
					</execution>

					<execution>
						<id>default</id>
						<phase>pre-integration-test</phase>
						<goals>
							<goal>run</goal>
						</goals>
						<configuration>
							<tasks>
								<property name="compile_classpath" refid="maven.compile.classpath" />
								<property name="runtime_classpath" refid="maven.runtime.classpath" />
								<property name="test_classpath" refid="maven.test.classpath" />
								<property name="plugin_classpath" refid="maven.plugin.classpath" />

								<echo level="info">Loading databases</echo>

								<ant antfile="${ant.build.dir}/build-parent.xml" target="database-clean">
									<property file="${ant.target.dir}/build.${user.name}.properties" />
									<property file="${ant.target.dir}/build.properties" />
									<property name="parent.dir" value="${ant.build.dir}"/>
									<property name="parent.target.dir" value="${ant.target.dir}"/>
									<property name="bbdd.dir" value="./target/classes/schemas"/>
								</ant>

								<echo level="info">Finish load databases</echo>

							</tasks>
						</configuration>
					</execution>
				</executions>
				<dependencies>
				
					<dependency>
						<groupId>${jdbc.groupId}</groupId>
						<artifactId>${jdbc.artifactId}</artifactId>
						<version>${jdbc.version}</version>
					</dependency>

					<dependency>
						<groupId>org.apache.ant</groupId>
						<artifactId>ant</artifactId>
						<version>1.7.0</version>
					</dependency>

					<dependency>
						<groupId>org.apache.ant</groupId>
						<artifactId>ant-nodeps</artifactId>
						<version>1.7.0</version>
					</dependency>

					<dependency>
						<groupId>org.apache.ant</groupId>
						<artifactId>ant-launcher</artifactId>
						<version>1.7.0</version>
					</dependency>

				</dependencies>

			</plugin>
		</plugins>

	</build>

	<properties>
			<ant.build.dir>../../../scripts/src/main/resources/common/ant</ant.build.dir>
			<ant.target.dir>../../../scripts/target/classes/ant</ant.target.dir>
			
		<jdbc.test.databaseName>djimenez_test</jdbc.test.databaseName>
	</properties>
</project>
